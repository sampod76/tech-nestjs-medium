model Post {
    id        String   @id @default(uuid())
    title     String
    category  String
    content   String
    author    String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    deletedAt DateTime?

    @@index([deletedAt])
    @@map("posts")
    @@schema("posts")
}
